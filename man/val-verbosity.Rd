% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbosity.R
\name{val-verbosity}
\alias{val-verbosity}
\title{Console verbosity control for \verb{val_*} functions}
\description{
\code{val.pipeline} emits a large volume of \code{cat()}-based progress messages
during a full \code{val_pipeline()} run. These helpers give users a single
knob (\code{verbose}) to dial that output up or down without editing source
code, and give the package a drop-in \code{cat()} replacement (\code{val_msg()})
that respects the current tier.
}
\section{Tiers}{
\itemize{
\item \code{"quiet"} (0) — silent. Only \code{warning()} / \code{stop()} still fire.
\item \code{"minimal"} (1) — pipeline + phase banners, a one-line summary per
package as it lands (name, version, risk decision, elapsed time),
and top-level count / runtime summary lines. Intended for
production runs where console output should be scannable.
\item \code{"normal"} (2, the default) — everything \code{val.pipeline} emitted
prior to the introduction of \code{verbose}. Matches the pre-existing
console output so users upgrading in place see no change unless
they opt in.
\item \code{"verbose"} (3) — every \verb{--> downloaded / untarred / initial reference complete / meta bundle saved} crumb plus per-metric
auto-accept diagnostics. Intended for debugging odd behaviour on a
single package.
}
}

\section{Setting the tier}{
Either pass \code{verbose = "minimal"} (etc.) explicitly to each
\code{val_pipeline()} / \code{val_build()} / \code{val_pkg()} / \code{val_categorize()} /
\code{val_decision()} / \code{val_pipeline_report()} call, or set a session
default with \code{options(val.pipeline.verbose = "minimal")}. The
function argument wins when both are set.
}

\keyword{internal}
