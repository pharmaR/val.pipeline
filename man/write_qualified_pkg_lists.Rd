% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{write_qualified_pkg_lists}
\alias{write_qualified_pkg_lists}
\title{Write Qualified Package Lists per Source}
\usage{
write_qualified_pkg_lists(
  qual_metadata,
  out_dir,
  qualified_decision = pull_config(val = "decisions_lst", rule_type = "default")[1]
)
}
\arguments{
\item{qual_metadata}{A data.frame with at least \code{pkg} and
\code{final_decision} columns, plus either \code{repo_name} (preferred) or
the older \code{repos} column (which stores the install URL and can be
reverse-engineered via \code{\link[=get_repo_origin]{get_repo_origin()}}).}

\item{out_dir}{Character(1). Directory to write the text files into.
Created (recursively) if it doesn't yet exist.}

\item{qualified_decision}{Character(1). The \code{final_decision} value
that marks a package as qualified for the validated repo. Defaults
to the lowest-risk decision from the config (typically \code{"Low"}).}
}
\value{
Invisibly, a named character vector: names are the source
labels (e.g. \code{"CRAN"}, \code{"BioC"}), values are the absolute paths of
the text files written. Empty if no qualified packages exist.
}
\description{
Given a \code{qual_metadata} data frame (typically the object saved by
\code{\link[=val_build]{val_build()}} to \code{qual_metadata.rds}), write one newline-delimited
text file per source (\code{repo_name}) into \code{out_dir}. Each file is named
\verb{qualified-<source>.txt} and contains, one package per line, the
names of every package whose \code{final_decision} matches
\code{qualified_decision}. \verb{<source>} is the value of the \code{repo_name}
column produced by \code{\link[=val_pkg]{val_pkg()}} — typically \code{CRAN}, \code{BioC}, or
\code{github} (every non-CRAN/BioC github-hosted source is normalised to
a single \code{github} bucket by \code{\link[=get_repo_origin]{get_repo_origin()}}).
}
\details{
These files are intended to be consumed as source configurations for
the "validated" repository provisioned into a Posit Package Manager
(PPM) instance in a GxP environment. Because PPM's source-file
configuration expects one package name per line, no header, no
quoting, and no trailing metadata, this helper deliberately writes a
plain, alphabetised list.

Rows whose \code{repo_name} is \code{NA} or \code{"unknown"} are folded into a
single \code{qualified-NA.txt} file so no qualified package ever silently
drops out of provisioning. An informative message is emitted so an
operator can investigate and re-route those packages if needed.

If \code{qual_metadata} predates the \code{repo_name} column (i.e. was
produced by an older \code{val_build()}), the helper will reverse-engineer
it row-by-row from the \code{repos} install URL by round-tripping through
\code{\link[=get_repo_origin]{get_repo_origin()}} against the current session's
\code{getOption("repos")}. Any URL that no longer maps to a configured
repo resolves to \code{"unknown"} and lands in \code{qualified-NA.txt}. If
neither \code{repo_name} nor \code{repos} is present the helper errors — the
source of every qualified package can't be recovered from thin air.
}
\keyword{internal}
