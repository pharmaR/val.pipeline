% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioc.R
\name{configure_bioc_repositories}
\alias{configure_bioc_repositories}
\title{Configure BiocManager to use only user-supplied repositories}
\usage{
configure_bioc_repositories(repos = NULL, quiet = FALSE)
}
\arguments{
\item{repos}{Optional named character vector of repository URLs. When
supplied it is used as the shim's return value; when \code{NULL} (the
default) the shim calls \code{getOption("repos")} each time it is
invoked, so any later \code{options(repos = ...)} change is picked up
automatically.}

\item{quiet}{Logical. When \code{FALSE} (the default) a short informational
message is emitted describing the effective repositories.}
}
\value{
Invisibly, the character vector of repository URLs that
\code{BiocManager::repositories()} will now return.
}
\description{
Some validation environments (typically air-gapped Posit Package Manager
installations) cannot reach \code{bioconductor.org}. In that situation
\code{BiocManager::repositories()} still emits its five hard-coded public
BioC URLs (\code{BioCsoft}, \code{BioCann}, \code{BioCexp}, \code{BioCworkflows},
\code{BioCbooks}) in addition to whatever the user has set on
\code{options("repos")}, and downstream calls in \code{riskmetric} — for
example \code{assess_reverse_dependencies()} — fail with
}
\details{
\preformatted{
Bioconductor version cannot be validated; no internet connection? ...
cannot open the connection to 'https://bioconductor.org/packages/.../VIEWS'
}

This helper puts an in-session shim in place so that
\code{BiocManager::repositories()} returns \emph{only} the caller's
\code{options("repos")} value. It is intentionally opt-in and it does not
mutate any package on disk.

Two ways to opt in:
\itemize{
\item Call \code{configure_bioc_repositories()} (or the wrapper
\code{\link[=configure_bioc_repositories_if_requested]{configure_bioc_repositories_if_requested()}}) yourself, before
running any \code{val.pipeline} entry point.
\item Set the environment variable \code{VAL_PIPELINE_INTERNAL_BIOC=1}
(or \code{TRUE}, or \code{true}). The \code{val.pipeline} entry points call
\code{configure_bioc_repositories_if_requested()} at startup, so the
shim is applied for the run without any code change.
}

The shim also sets
\code{options(BiocManager.check_repositories = FALSE)} so that BiocManager
does not phone home to \code{bioconductor.org} to validate the release
version. If the caller has not set the \code{R_BIOC_VERSION} environment
variable, this helper leaves it untouched — that is the caller's
responsibility (typical value: \code{"3.22"}).

If the caller's repo vector contains a single flat \code{BioC} entry
(typical Posit Package Manager layout, where the whole BioC snapshot
lives under one URL rather than being split into \code{BioCsoft},
\code{BioCann}, \code{BioCexp}, \code{BioCworkflows}, \code{BioCbooks}), the shim also
auto-aliases any missing \verb{BioC*} names to the same URL. This keeps
downstream callers that index by name — for example
\code{BiocManager::repositories()[["BioCsoft"]]} inside \code{riskmetric}'s
reverse-dependency assessment — working out of the box.
}
\examples{
\dontrun{
# Point R at the internal PPM CRAN + BioC snapshots first
options(repos = c(
  CRAN = "https://ppm.example.com/cran/latest",
  BioC = "https://ppm.example.com/bioc/latest"
))
Sys.setenv(R_BIOC_VERSION = "3.22")

# Then install the shim
configure_bioc_repositories()

# BiocManager now returns only the internal repos
BiocManager::repositories()
}

}
\seealso{
\code{\link[=configure_bioc_repositories_if_requested]{configure_bioc_repositories_if_requested()}}
}
