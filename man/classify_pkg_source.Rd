% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{classify_pkg_source}
\alias{classify_pkg_source}
\title{Classify a Package's Source of Origin (URL-first, DESCRIPTION-fallback)}
\usage{
classify_pkg_source(repo_src = NULL, pkg_name = NULL, desc_path = NULL)
}
\arguments{
\item{repo_src}{Character. Repository URL (with or without
\verb{/src/contrib}) — passed straight through to \code{\link[=get_repo_origin]{get_repo_origin()}}.
Can be \code{NULL} to skip step 1 and go straight to DESCRIPTION.}

\item{pkg_name}{Character(1). Package name, used only for warning
messages emitted by \code{\link[=get_repo_origin]{get_repo_origin()}}.}

\item{desc_path}{Character(1) or \code{NULL}. Path to the DESCRIPTION
file inspected in steps 2-5. May also be a directory containing
\code{DESCRIPTION}, or the path to an extracted source tree. When
\code{NULL} or the file is unreadable, steps 2-5 are skipped.}
}
\value{
Character(1). One of \code{"CRAN"}, \code{"BioC"}, \code{"github"}, or
\code{"unknown"} (or whatever custom label the URL match in step 1
resolved to when the user has a custom-labelled entry in
\code{opt_repos}).
}
\description{
Determine whether a package came from CRAN, Bioconductor (\code{"BioC"}),
or GitHub (\code{"github"}), returning \code{"unknown"} when neither signal
resolves. Designed to survive a merged Posit Package Manager (PPM)
repo where every package appears to come from a single URL — in that
scenario the URL match is uninformative and the DESCRIPTION fallback
does the real work.
}
\details{
Priority order (first hit wins):
\enumerate{
\item URL match — delegates to \code{\link[=get_repo_origin]{get_repo_origin()}} with
\code{names_only = TRUE}. If it returns anything other than
\code{"unknown"}, that label is returned. This preserves
backwards-compatibility for existing multi-entry \code{opt_repos}
configurations.
\item \code{biocViews} present and non-empty in DESCRIPTION → \code{"BioC"}.
This field is mandatory for Bioconductor packages and
effectively never present on CRAN/GitHub packages.
\item \code{Repository: CRAN} in DESCRIPTION → \code{"CRAN"}. CRAN adds
this field to the tarballs it publishes.
\item \code{RemoteType: github} in DESCRIPTION → \code{"github"}. Written
by \code{remotes::install_github()} / \code{pak} when installing from
GitHub.
\item \code{URL} or \code{BugReports} contains a \verb{github.com/<owner>/<repo>}
pattern → \code{"github"}. Weakest signal (many CRAN packages also
list a GitHub URL for issue tracking), applied only after
\code{biocViews} / \code{Repository: CRAN} have been ruled out.
\item Fallback → \code{"unknown"}.
}
}
\examples{
\dontrun{
# In a val_pkg() context — pass the extracted source's DESCRIPTION
# as the fallback source of truth:
classify_pkg_source(
  repo_src = "https://packagemanager.posit.co/validated/latest",
  pkg_name = "admiral",
  desc_path = file.path(out_dir, "sourced", "admiral", "DESCRIPTION")
)
}

}
\keyword{internal}
