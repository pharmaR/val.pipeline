% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mem_watchdog.R
\name{append_watchdog_row}
\alias{append_watchdog_row}
\title{Append one row to the mem_watchdog TSV}
\usage{
append_watchdog_row(path, row)
}
\arguments{
\item{path}{Character(1). Path to the TSV, e.g. \verb{<val_dir>/mem_watchdog.tsv}.}

\item{row}{Named list / one-row data frame. Must include the columns
listed in \code{mem_watchdog_cols()}.}
}
\value{
\code{TRUE} on success, \code{FALSE} on any I/O failure (silently — the
watchdog must never take a run down).
}
\description{
Creates the file with a header on first write, then appends thereafter.
Concurrent worker appends are safe: writes are one line, fit in
PIPE_BUF, and use \code{O_APPEND} semantics via \code{\link[=cat]{cat()}} with \code{append = TRUE}.
}
\keyword{internal}
