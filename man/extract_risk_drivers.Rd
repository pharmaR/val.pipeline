% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{extract_risk_drivers}
\alias{extract_risk_drivers}
\title{Extract Risk Drivers From a Decision Row}
\usage{
extract_risk_drivers(decision_df, decisions = c("Low", "Medium", "High"))
}
\arguments{
\item{decision_df}{A single-row \code{data.frame} with a \code{final_risk} column
(factor or character) and one or more \verb{<metric>_cat} columns.}

\item{decisions}{Character vector of decision categories, lowest to highest.
Metrics matching \code{decisions[1]} (the "safe" category) are never returned
as drivers.}
}
\value{
A single character string with driver metric names separated by
\code{", "}, or \code{NA_character_} if none apply (e.g. \code{final_risk} is the
lowest category, is \code{NA}, or no \verb{<metric>_cat} column matches).
}
\description{
Given a single-row \code{data.frame} produced by \code{\link[=val_decision]{val_decision()}} (or one of the
underlying \verb{rip_cats_*} helpers), return the names of the per-metric
categorization columns (\verb{<metric>_cat}) whose category equals the
\code{final_risk} for that row. Only metrics that "tie" the final risk category
are returned — i.e. the metrics that drove the package out of the lowest
risk category.
}
\details{
Used by \code{\link[=val_pkg]{val_pkg()}} to populate \code{decision_reason_note} with the specific
metrics that caused a package to fall out of the lowest-risk category into
\code{"Medium"} / \code{"High"}.
}
\keyword{internal}
