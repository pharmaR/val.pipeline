% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{apply_config_path}
\alias{apply_config_path}
\title{Apply a user-supplied config path for the duration of a call}
\usage{
apply_config_path(config_path)
}
\arguments{
\item{config_path}{Path to a \code{config.yml}. If \code{NULL}, nothing is done
and \code{NULL} is returned.}
}
\value{
The previous value of the \code{val.pipeline.config_path} option in
the form returned by \code{\link[base:options]{base::options()}} (suitable for feeding back
into \code{options()}), or \code{NULL} when \code{config_path} is \code{NULL}.
}
\description{
Sets the \code{val.pipeline.config_path} session option to a validated,
normalized path so \code{\link[=pull_config]{pull_config()}} can pick up the user override
anywhere inside the call graph. Returns the prior option value so the
caller can restore it via \code{on.exit(options(old))}.
}
\keyword{internal}
