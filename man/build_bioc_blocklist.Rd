% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_bioc_blocklist.R
\name{build_bioc_blocklist}
\alias{build_bioc_blocklist}
\title{Build a complete BioC blocklist against the full available.packages() universe}
\usage{
build_bioc_blocklist(
  qual_metadata,
  opt_repos = NULL,
  config_path = NULL,
  qualified_decision = NULL,
  min_universe = 100L
)
}
\arguments{
\item{qual_metadata}{A \code{qual_metadata} data frame (as produced by
\code{\link[=val_build]{val_build()}}). Must contain \code{pkg} (or \code{package}) and
\code{final_decision}; \code{repo_name} is used when present.}

\item{opt_repos}{A named list/character of repo alias -> URL. If
\code{NULL} (default), read from the effective config via
\code{\link[=pull_config]{pull_config()}}.}

\item{config_path}{Optional path to a \code{config.yml}. Forwarded to
\code{\link[=pull_config]{pull_config()}} when \code{opt_repos} is \code{NULL}. Prefer the run's
snapshotted config (e.g. \verb{<val_dir>/config.yml}) when auditing a
historical run so BioC versioning is pinned consistently.}

\item{qualified_decision}{Character(1). The \code{final_decision} value
that marks a package as qualified. Defaults to the lowest-risk
decision from the config (typically \code{"Low"}).}

\item{min_universe}{Integer(1). Minimum acceptable universe size.
Guards against a config typo silently producing an empty
universe. Defaults to \code{100L}; real BioC universes are 2k+.}
}
\value{
A data frame with columns \code{package}, \code{version},
\code{matched_repo}, \code{reason} (\code{"not_assessed"} or
\code{"assessed_<risk>"}), sorted by reason then package. Zero rows
is possible but implausible; a warning is emitted in that case.
}
\description{
Returns the set of Bioconductor packages that should be blocked
from a validated repository (e.g. Posit Package Manager), given a
pipeline's \code{qual_metadata}. The blocklist is computed as
}
\details{
\preformatted{
blocklist = BioC_universe - Low_risk_allowlist
          = (dropped_at_remote_reduce U never_seen_by_pipeline) +
            (assessed_but_not_Low)
}

where \code{BioC_universe} is pulled live via \code{\link[utils:available.packages]{utils::available.packages()}}
against whatever repos in the effective \code{opt_repos} config look
like BioC (case-insensitive substring \code{"bioc"} match on \strong{both} the
repo alias and URL). This intentionally covers non-OSS forks that
consolidate the four BioC sub-repos into a single entry, since both
the alias and URL contain \code{"bioc"}.

Why this exists: \code{\link[=write_qualified_pkg_lists]{write_qualified_pkg_lists()}}'s legacy behaviour
was to write \code{blocklist-BioC.txt} as the inverse of the
\emph{assessed} BioC set only. A large fraction of the BioC universe is
dropped at \code{remote_reduce} and never seen by the pipeline; those
packages were absent from both the allowlist and the blocklist,
so PPM would silently serve them. This function closes that leak.

\code{write_qualified_pkg_lists(use_full_universe = TRUE)} now calls
this internally when writing \code{blocklist-BioC.txt}. Call this
directly if you want the blocklist as a data frame with reason
codes for auditing or reporting.
}
\examples{
\dontrun{
qm <- readRDS("/data/shared/riskassessments/R_4.5.2/20260730/qual_metadata.rds")
bl <- build_bioc_blocklist(
  qm,
  config_path = "/data/shared/riskassessments/R_4.5.2/20260730/config.yml"
)
table(bl$reason)
}

}
\seealso{
\code{\link[=write_qualified_pkg_lists]{write_qualified_pkg_lists()}}, \code{\link[=is_bioc_repo]{is_bioc_repo()}}.
}
