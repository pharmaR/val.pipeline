% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_pkg_tree.R
\name{resolve_pkg_tree}
\alias{resolve_pkg_tree}
\title{Resolve a Package Set into a Dependency-Ordered Build List}
\usage{
resolve_pkg_tree(
  pkg_names,
  deps = c("depends", "suggests")[1],
  deps_recursive = TRUE,
  avail_pkgs = NULL
)
}
\arguments{
\item{pkg_names}{Character vector of seed package names. When
\code{NULL} (only meaningful for \code{val_build()}'s legacy
assess-everything-on-CRAN path), the returned \code{pkgs} is every
package in \code{avail_pkgs} and no dep expansion is performed.}

\item{deps}{One of \code{"depends"}, \code{"suggests"}, \code{c("depends","suggests")},
or \code{NULL}. Passed through to the \code{which} argument of
\code{\link[tools:package_dependencies]{tools::package_dependencies()}}. \code{NULL} means "don't expand — use
\code{pkg_names} as-is".}

\item{deps_recursive}{Logical. Whether dependency expansion is
recursive. Passed straight through.}

\item{avail_pkgs}{Optional data frame from
\code{as.data.frame(available.packages())}. When \code{NULL}, one is
fetched via \code{utils::available.packages()}. Callers that already
hold one can pass it in to avoid a round-trip.}
}
\value{
A list with:
\describe{
\item{pkgs}{Character vector of package names in build order
(descending dependency frequency, ties broken alphabetically).}
\item{vers}{Character vector of versions, aligned to \code{pkgs}.}
\item{avail_pkgs}{The dep-frequency-sorted \code{available.packages()}
data frame. Retains a \code{dep_freq} column when dep expansion
actually happened.}
}
}
\description{
Given a seed set of packages and a dependency policy, expands the
set into its full (optionally recursive) dependency tree via
\code{\link[tools:package_dependencies]{tools::package_dependencies()}}, then sorts the resulting universe
by descending dependency frequency so foundational packages get
assessed first. Returns the aligned name / version / avail_pkgs
triple that both \code{\link[=val_prep_pipeline]{val_prep_pipeline()}} and \code{\link[=val_build]{val_build()}} need to
drive their per-package loops.
}
\details{
This is the shared workhorse behind the "Which pkgs, ordered" block
that used to be duplicated in the top of \code{val_build()} and the
bottom of \code{val_prep_pipeline()}.
}
\keyword{internal}
