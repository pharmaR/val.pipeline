% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbosity.R
\name{val_log_at_least}
\alias{val_log_at_least}
\title{Is the current log-file tier at least \code{min_level}?}
\usage{
val_log_at_least(min_level = "normal")
}
\arguments{
\item{min_level}{Character(1), one of
\code{c("quiet","minimal","normal","verbose")}.}
}
\value{
\code{TRUE} when the log-file tier is at least \code{min_level}.
}
\description{
Reads the session option \code{val.pipeline.log_level} (falling back to
\code{"normal"}) and compares its tier index against \code{min_level}. The
log-file tier is intentionally decoupled from the console tier:
users often want a quiet console (\code{verbose = "minimal"}) alongside
a rich on-disk log (\code{log_level = "normal"} or \code{"verbose"}).
}
\keyword{internal}
