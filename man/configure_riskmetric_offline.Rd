% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioc.R
\name{configure_riskmetric_offline}
\alias{configure_riskmetric_offline}
\title{Route \code{riskmetric}'s Bioconductor lookups through internal repos}
\usage{
configure_riskmetric_offline(quiet = FALSE)
}
\arguments{
\item{quiet}{Logical. When \code{FALSE} (the default) a short informational
message is emitted.}
}
\value{
\code{TRUE} invisibly on success, \code{FALSE} invisibly when the
\code{riskmetric} package is not installed.
}
\description{
Installs four in-session shims on \code{riskmetric} so its Bioconductor-facing
code paths stop reaching out to public \code{bioconductor.org} URLs and
instead consult \code{options("repos")} — i.e. the same internal PPM CRAN
and BioC snapshots the rest of \code{val.pipeline} uses.
}
\section{Shim 1: \code{assess_reverse_dependencies.default}}{
\code{riskmetric::assess_reverse_dependencies.default()} calls
\code{devtools::revdep(x$name, bioconductor = TRUE)}, which in turn calls
\code{devtools:::bioc_packages()}. That helper unconditionally reads a
\code{VIEWS} file from \code{BiocManager::repositories()[["BioCsoft"]]}. On a
Posit Package Manager mirror the aggregated BioC snapshot is served
at \verb{<repo>/src/contrib/PACKAGES} — there is no \verb{<repo>/VIEWS} file at
the mirror root — so the read fails with

\preformatted{
cannot open the connection to '.../bioc-.../latest/VIEWS'
}

The shim replaces it with a version that computes reverse
dependencies from \code{utils::available.packages()} +
\code{tools::dependsOnPkgs()}. No \code{VIEWS} file is required.
}

\section{Shim 2: \code{memoise_bioc_available}}{
\code{riskmetric:::memoise_bioc_available()} hard-codes
\code{read.dcf(url("https://bioconductor.org/packages/release/bioc/src/contrib/PACKAGES"))}
to build its Bioconductor package index. That URL is contacted
\emph{directly}, bypassing \code{BiocManager::repositories()}, so
\code{\link[=configure_bioc_repositories]{configure_bioc_repositories()}} alone can't rescue it and
\code{pkg_ref("<BioCPkg>")} fails on an air-gapped host with

\preformatted{
cannot open the connection to 'https://bioconductor.org/packages/release/bioc/src/contrib/PACKAGES'
}

The shim replaces it with a version that queries the set of
Bioconductor repos advertised to the session and passes them to
\code{utils::available.packages()}. That set is resolved, in order of
specificity, from:

\enumerate{
\item \code{options("val.pipeline.bioc_repos")} — explicit override
(named \code{character} or single URL);
\item \code{Sys.getenv("VAL_PIPELINE_BIOC_REPOS")} — comma-separated URLs;
\item \code{BiocManager::repositories()} entries whose names begin \code{BioC};
\item \code{options("repos")} entries whose name begins \code{BioC} \emph{or}
whose URL contains \code{bioconductor} / \verb{/bioc/} (case-insensitive);
\item \code{options("repos")} in full, as a last resort.
}

Step 4 lets consolidated PPM setups — where one URL serves both CRAN
and BioC snapshots — work without having to synthesise five fake
\verb{BioC*} entries. The result is memoised with \code{memoise::memoise()},
matching the upstream contract, so repeated \code{pkg_ref()} calls hit
the cache. Upstream fix proposed at
\href{https://github.com/pharmaR/riskmetric/pull/402}{pharmaR/riskmetric#402}.
}

\section{Shim 3: \code{is_available_cran}}{
\code{riskmetric:::verify_pkg_source()} calls \code{is_available_cran()} before
\code{is_available_bioc()}. When one consolidated PPM repo advertises both
CRAN and Bioconductor packages, BioC packages such as \code{BiocGenerics}
are found by the CRAN check first and classified as \code{pkg_cran_remote}
— which then breaks Bioconductor-specific downstream code (for
example the \code{has_examples} metric has no \code{pkg_cran_remote} method,
and reverse-dependency lookups follow the wrong code path). The shim
wraps \code{is_available_cran} so any package that is also present in
\code{memoise_bioc_available()} no longer counts as CRAN, and dispatch
falls through to \code{is_available_bioc()}. \code{pkg_ref("BiocGenerics")}
then correctly returns a \code{pkg_bioc_remote}.
}

\section{Shim 4: \code{pkg_bioc}}{
Released \code{riskmetric::pkg_bioc()} builds the \code{pkg_bioc_remote}
reference with a hard-coded
\code{repo = "https://bioconductor.org/packages/release/bioc"}, discarding
the \code{Repository} column returned by \code{memoise_bioc_available()}. Every
downstream metric that follows \code{x$repo_base_url} — \code{has_news},
\code{remote_checks}, \code{news_current}, \code{has_vignettes}, \code{has_maintainer},
\code{bugs_status}, \code{has_source_control}, \code{has_bug_reports_url}, \code{license},
and friends — then fetches from \code{bioconductor.org} and fails with

\preformatted{
Failed to connect to bioconductor.org port 443: Connection refused
}

on air-gapped hosts. The shim replaces \code{pkg_bioc()} with a version
that uses the \code{Repository} column from the shimmed
\code{memoise_bioc_available()} (i.e. the internal PPM BioC URL), so
\code{x$repo_base_url} points at the mirror and every derived scrape hits
the mirror too. Mirrors the upstream fix at
\href{https://github.com/pharmaR/riskmetric/pull/402}{pharmaR/riskmetric#402}.

All four shims are intentionally opt-in; the wrapper
\code{\link[=configure_riskmetric_offline_if_requested]{configure_riskmetric_offline_if_requested()}} runs this helper only
when the run is flagged as air-gapped (env var
\code{VAL_PIPELINE_INTERNAL_BIOC} truthy, or \code{default: air_gapped: true}
in \code{config.yml}), so public-network users are unaffected.
}

\seealso{
\code{\link[=configure_riskmetric_offline_if_requested]{configure_riskmetric_offline_if_requested()}},
\code{\link[=configure_bioc_repositories]{configure_bioc_repositories()}}
}
