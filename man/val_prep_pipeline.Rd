% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/val_prep_pipeline.R
\name{val_prep_pipeline}
\alias{val_prep_pipeline}
\title{Validation: Prep Stage of the Assessment Pipeline}
\usage{
val_prep_pipeline(
  ref = c("source", "remote"),
  metric_pkg = c("riskmetric", "val.meter", "risk.assessr"),
  deps = c("depends", "suggests")[1],
  deps_recursive = TRUE,
  val_date = Sys.Date(),
  out = Sys.getenv("RISK_OUTPATH", unset = getwd()),
  opt_repos = c(CRAN = "https://packagemanager.posit.co/cran/latest", BioC =
    "https://bioconductor.org/packages/3.22/bioc"),
  verbose = NULL,
  toml_path = NULL,
  toml_project_name = "val.pipeline run"
)
}
\arguments{
\item{ref}{Character. Source of the packages. Default is "source". Options
are "source" or "remote".}

\item{metric_pkg}{Character. The package used for metrics. Default is
"riskmetric".}

\item{deps}{Character or NULL. Types of dependencies to consider. Default is
"depends". Options include "depends", "suggests", or NULL.}

\item{deps_recursive}{Logical. Whether to consider dependencies recursively.
Default is TRUE.}

\item{val_date}{Date. The date for validation. Default is the current date.}

\item{out}{Character. Output directory for assessments. Default is
Sys.getenv("RISK_OUTPATH", unset = getwd()).}

\item{opt_repos}{Named character vector. Repositories to use. Default is
opt_repos from config.}

\item{verbose}{Console verbosity control. One of \code{"quiet"},
\code{"minimal"}, \code{"normal"} (default), or \code{"verbose"}. See
the \code{val.pipeline} verbosity docs for tier definitions. Also accepts an integer 0-3
or a logical (\code{TRUE} = \code{"normal"}, \code{FALSE} = \code{"quiet"}). The
session option \code{val.pipeline.verbose} is used when this argument
is left as the default and set to \code{NULL}.}

\item{toml_path}{Character or \code{NULL}. Where to write the
\code{pipeline.toml} file. Defaults to \code{file.path(val_dir, "pipeline.toml")}
where \code{val_dir} is the run's date-stamped output directory.}

\item{toml_project_name}{Character(1). Value written to the toml's
\verb{[project].name} field. Defaults to \code{"val.pipeline run"}.}
}
\value{
A list of class \code{"val_prep"} containing:
\describe{
\item{pkgs}{Character vector of package names, sorted in the
dependency-frequency order \code{val_build()} uses.}
\item{vers}{Character vector of package versions, aligned to \code{pkgs}.}
\item{avail_pkgs}{The dep-frequency-sorted \code{available.packages()}
data frame \code{val_build()} needs to look up repository sources.}
\item{val_dir}{The run's output directory
(\verb{<out>/R_<r_ver>/<YYYYMMDD>}).}
\item{val_date, val_start, r_ver}{Timing / version metadata.}
\item{opt_repos}{Named character vector of the val-date-adjusted
repositories.}
\item{decisions, viable_metrics}{Config snapshots picked up from
\code{pull_config()}.}
\item{pre_filtered_pkg_metrics}{The full \code{val_categorize()} output.}
\item{n_candidates}{Number of rows in \code{pre_filtered_pkg_metrics}
(used later by the summary report).}
\item{toml_path}{Path to the written \code{pipeline.toml}.}
}
}
\description{
Runs everything \code{val_pipeline()} normally does \emph{before} the per-package
assessment loop kicks off — pulling config, evaluating viable metrics,
running the pre-filter (\code{val_categorize()}), applying the primary
filter + \code{pass_primary} inclusion, and then resolving the full
(optionally recursive) dependency tree via \code{\link[tools:package_dependencies]{tools::package_dependencies()}}
so the final ordered list of packages that \code{val_build()} would iterate
over is materialised eagerly. Finally, it writes that list out as a
\code{pipeline.toml} file suitable for feeding to \code{rv} so a Posit Package
Manager snapshot can be installed \emph{before} the (expensive) build step
begins.
}
\details{
The returned object can be fed straight into \code{val_pipeline(prep = ...)}
(or \code{val_build(prep = ...)}) to skip the prep phase on the second leg
of the run.
}
\seealso{
\code{\link[=val_pipeline]{val_pipeline()}} for the one-shot wrapper that composes the
prep + build phases, and \code{\link[=write_pipeline_toml]{write_pipeline_toml()}} for the underlying
toml writer.
}
