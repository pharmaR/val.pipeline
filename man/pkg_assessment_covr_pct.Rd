% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pkg_assessment_covr_pct}
\alias{pkg_assessment_covr_pct}
\title{Safely Extract \code{covr_coverage$totalcoverage} from a Package Assessment}
\usage{
pkg_assessment_covr_pct(pkg_assessment)
}
\arguments{
\item{pkg_assessment}{A \code{list_of_pkg_metric} as returned by
\code{\link[riskmetric:pkg_assess]{riskmetric::pkg_assess()}} (post-\code{\link[=strip_recording]{strip_recording()}}).}
}
\value{
Numeric(1) on 0-100 scale, or \code{NA_real_} when the field is
absent, mal-shaped, or errored.
}
\description{
\code{pkg_assessment} is a \code{vctrs::list_of_pkg_metric} immediately after
\code{riskmetric::pkg_assess()}, and vctrs' \code{$} / \code{[[} methods on that
class are strict: reading a name that isn't in the object throws
\verb{Invalid index: field name 'covr_coverage' not found} rather than
returning \code{NULL}. That's the exact shape auto-accepted and
remote-only packages produce, since \code{val_pkg()} intentionally drops
\code{assess_covr_coverage} from their metric set to save compute — so a
plain \code{pkg_assessment$covr_coverage$totalcoverage} aborts the whole
\code{val_pkg()} call before the value can even be checked.
}
\details{
This helper bypasses the vctrs method with \code{.subset2()}, guards the
inner \verb{$totalcoverage} access (a \code{pkg_metric_na} atomic scalar
would trigger a different "\code{$} operator is invalid for atomic
vectors" error), coerces to numeric, and returns a length-1 numeric
or \code{NA_real_}. Use anywhere in \code{val_pkg()} that needs to read the
coverage percent — do NOT reach into \code{pkg_assessment$covr_coverage}
directly.
}
\keyword{internal}
