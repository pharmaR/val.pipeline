% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_bioc_blocklist.R
\name{is_bioc_repo}
\alias{is_bioc_repo}
\title{Detect BioC entries in a named repos vector or list}
\usage{
is_bioc_repo(repos)
}
\arguments{
\item{repos}{Named character vector or list of \code{alias -> URL}.}
}
\value{
Logical vector the same length as \code{repos}.
}
\description{
Case-insensitive substring match on \code{"bioc"} against \strong{both} the
repo alias (\code{names(repos)}) and the URL (values). The dual-side
match handles two real-world cases the OSS \code{BioC} alias alone
would miss:
}
\details{
\itemize{
\item Non-OSS forks that consolidate the four BioC sub-repos
(\code{BioCsoft}, \code{BioCann}, \code{BioCexp}, \code{BioCwork}) into a single
entry whose alias and URL both contain \code{"bioc"}.
\item URL-only aliases (e.g. an internal mirror named \code{sci} whose URL
is a Bioconductor path).
}

Used by \code{\link[=build_bioc_blocklist]{build_bioc_blocklist()}} and by
\code{\link[=write_qualified_pkg_lists]{write_qualified_pkg_lists()}} when \code{use_full_universe = TRUE}.
}
\examples{
is_bioc_repo(c(CRAN = "https://cran.rstudio.com",
               BioC = "https://bioconductor.org/packages/3.22/bioc"))
}
