% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioc.R
\name{configure_bioc_repositories_if_requested}
\alias{configure_bioc_repositories_if_requested}
\title{Install the BiocManager repositories shim when requested by an env var}
\usage{
configure_bioc_repositories_if_requested(quiet = FALSE)
}
\arguments{
\item{quiet}{Passed through to \code{\link[=configure_bioc_repositories]{configure_bioc_repositories()}}.}
}
\value{
Invisibly, the character vector of repository URLs that the
shim will return (or \code{character(0)} when the env var is unset or
BiocManager is not installed).
}
\description{
Wrapper around \code{\link[=configure_bioc_repositories]{configure_bioc_repositories()}} that is a no-op unless
the environment variable \code{VAL_PIPELINE_INTERNAL_BIOC} is set to a
truthy value (\code{"1"}, \code{"TRUE"}, \code{"true"}, \code{"yes"}). This is what the
\code{val.pipeline} entry points call at startup so that public-network
users are unaffected while air-gapped users can opt in with a single
env var.
}
