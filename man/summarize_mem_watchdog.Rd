% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mem_watchdog.R
\name{summarize_mem_watchdog}
\alias{summarize_mem_watchdog}
\title{Summarize a \code{mem_watchdog.tsv} and (optionally) suggest a \code{workers} value}
\usage{
summarize_mem_watchdog(
  path,
  available_ram_gb = NULL,
  reserve_gb = 8,
  top_n = 10L
)
}
\arguments{
\item{path}{Character(1). Path to a \code{mem_watchdog.tsv}.}

\item{available_ram_gb}{Numeric(1) or \code{NULL}. Available RAM in GB used
to synthesize the \code{workers} suggestion. When \code{NULL}, uses whatever
\verb{MemAvailable:} from \verb{/proc/meminfo} reports (Linux only); otherwise
no suggestion is emitted.}

\item{reserve_gb}{Numeric(1). GB to hold back for OS + orchestrator R +
spike headroom. Defaults to 8.}

\item{top_n}{Integer(1). Rows to include in the "heaviest" summary
printed to console. Defaults to 10. The report template renders 25
independently.}
}
\value{
Invisibly, a list with fields \code{n}, \code{p50_mb}, \code{p95_mb}, \code{max_mb},
\code{top}, \code{suggested_workers} (may be \code{NULL}), and \code{sampler_mix}.
}
\description{
Called from \code{\link[=val_finalize]{val_finalize()}} to print a compact summary and, when the
current host's \code{free -g} output is available, suggest a \code{workers} value
for the next run keyed on the p95 per-pkg peak.
}
\keyword{internal}
