% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{reject_iteration}
\alias{reject_iteration}
\title{Reject Iteration: Propagate dependency-based decision downgrades}
\usage{
reject_iteration(pkg_dat, dec_reject, deps, decisions, failed_pkgs = NULL)
}
\arguments{
\item{pkg_dat}{A tibble with columns \code{pkg}, \code{decision}, \code{decision_reason},
\code{depends} (list-col of character), \code{suggests} (list-col of character).}

\item{dec_reject}{Character. The category to assign to dependency-failed
packages (typically \code{"High"}).}

\item{deps}{Character. The dependency scope from the enclosing
\code{val_build()} call (\code{"depends"} or \code{"suggests"}). Only when \code{"Suggests"}
is present will Suggests-failures propagate.}

\item{decisions}{Character vector. Ordered risk categories, low → high
(e.g. \code{c("Low", "Medium", "High")}). Only used to identify the "not
failed" baseline (\code{decisions[1]}) when \code{failed_pkgs} is \code{NULL}.}

\item{failed_pkgs}{Character vector of package names to treat as failed
on this iteration. If \code{NULL}, derived from \code{pkg_dat$final_decision != decisions[1]}.}
}
\value{
\code{pkg_dat} with \code{final_decision} and \code{final_decision_reason}
populated for every row (no \code{NA}s introduced by this function).
}
\description{
Given a data.frame of per-package risk assessments (one row per package,
with \code{decision}, \code{decision_reason}, \code{depends}, \code{suggests} list-columns),
compute the \code{final_decision} and \code{final_decision_reason} for every row by
marking any package whose dependencies (or Suggests, when \code{"Suggests"} is
in \code{deps}) appear in \code{failed_pkgs} as \code{dec_reject}. Pre-approved packages
are never downgraded. Packages with no failing dep carry their original
\code{decision} / \code{decision_reason} through to \code{final_decision} /
\code{final_decision_reason}.
}
\details{
The function is designed to be called iteratively from \code{val_build()} until
the set of failing packages stabilizes (a package can become "failed"
because one of its deps just became failed, which can cascade further).
}
\keyword{internal}
