% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkg_version_drift.R
\name{assess_pkg_version_drift}
\alias{assess_pkg_version_drift}
\title{Audit Per-Package Meta-Bundle Version Drift}
\usage{
assess_pkg_version_drift(
  val_dir,
  prep = NULL,
  avail_pkgs = NULL,
  verbose = TRUE
)
}
\arguments{
\item{val_dir}{Character(1). Path to the run directory (the parent
of \verb{assessed/}). Same shape as the output of \code{val_build()}.}

\item{prep}{Optional \code{val_prep} list from \code{val_prep_pipeline()}.
Preferred source of expected versions.}

\item{avail_pkgs}{Optional data.frame with \code{Package} and \code{Version}
columns (same shape as \code{available.packages()} output). Used when
\code{prep} is not supplied.}

\item{verbose}{Logical(1). Print a status count summary. Default
\code{TRUE}.}
}
\value{
A tibble with one row per package (union of on-disk +
expected sets), columns:
\itemize{
\item \code{pkg}: package name
\item \code{expected_ver}: character or \code{NA} when the pkg isn't in the
dispatch set
\item \code{on_disk_vers}: comma-separated on-disk versions, or \code{NA} when
no bundle exists
\item \code{n_on_disk}: integer count of on-disk bundles
\item \code{status}: one of \code{"match"}, \code{"drifted"}, \code{"multi_version"},
\code{"unexpected"}, \code{"missing"}
}
}
\description{
Compare the \verb{<pkg>_<ver>_meta.rds} files on disk under
\verb{val_dir/assessed/} against the packages + versions that \code{val_build()}
\emph{would} dispatch on the next run, and classify each mismatch. Useful
when \code{val_build()}'s cached-skip count (based on pkg + version) is
smaller than \code{length(list.files(assessed, pattern = "_meta.rds$"))}
(based on pkg-name alone) and you want to see exactly which packages
account for the gap.
}
\details{
The "expected version" is resolved in this order:
\enumerate{
\item \code{prep$vers} when a \code{val_prep} object is supplied (single source of
truth used by \code{val_build()}).
\item \code{avail_pkgs$Version} (indexed by \code{avail_pkgs$Package}) when
\code{prep} is \code{NULL} but \code{avail_pkgs} is supplied.
\item A fresh \code{available.packages()} call using the caller's current
\code{options("repos")} when neither is supplied. Cheap safety net;
prefer 1 or 2 when auditing a run whose snapshot repos differ
from the current session's.
}

Each package with an on-disk bundle or an expected version is
classified into one of:
\itemize{
\item \code{"match"}: on-disk version equals the expected version.
\item \code{"drifted"}: at least one on-disk bundle exists at a version
different from the expected one. The old bundle is stale; the pkg
will be re-dispatched by \code{val_build()} on the next run.
\item \code{"multi_version"}: two or more on-disk versions for the same
package (the run wrote multiple bundles across resumes). At least
one is stale.
\item \code{"unexpected"}: the pkg has an on-disk bundle but no expected
version (e.g. the pkg is no longer in the current dispatch set).
\item \code{"missing"}: the pkg has an expected version but no on-disk
bundle (never assessed).
}
}
\examples{
\dontrun{
# After a run finished (or partially finished):
prep <- val_prep_pipeline(...)
drift <- assess_pkg_version_drift(val_dir = "riskassessment/R_4.5.2/20260812",
                                  prep = prep)
# Show only mismatches:
subset(drift, status != "match")
}

}
