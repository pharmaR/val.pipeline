% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pull_covr_env_vars}
\alias{pull_covr_env_vars}
\title{Pull the covr Env-var Block for \code{val_pkg()}}
\usage{
pull_covr_env_vars(config_path = NULL)
}
\arguments{
\item{config_path}{Character(1) or \code{NULL}. Passed straight through to
\code{\link[=pull_config]{pull_config()}}; the same resolution rules apply.}
}
\value{
A named character vector. Names are env-var names, values are
the strings to set them to. Zero-length when the config block is
absent or empty.
}
\description{
Reads the \verb{default: covr_env_vars:} map from \code{inst/config.yml} (or a
user-supplied override, resolved the same way as \code{\link[=pull_config]{pull_config()}}) and
returns it as a named character vector suitable for
\code{\link[withr:with_envvar]{withr::with_envvar()}}.
}
\details{
\code{val_pkg()} uses the result to wrap the final \code{pkg_assess()} call so
that \code{assess_covr_coverage} runs under a controlled environment —
most importantly with \code{NOT_CRAN="true"} so \code{testthat::skip_on_cran()}
does not silently drop tests. See the block-level docstring on
\verb{covr_env_vars:} in \code{inst/config.yml} for the rationale.

The helper is deliberately tolerant of missing / empty configuration:
when the key is absent, \code{NULL}, or an empty list it returns a
zero-length named character vector, and \code{\link[withr:with_envvar]{withr::with_envvar()}} then
becomes a no-op wrapper (i.e. the parent session's environment is
inherited unchanged).
}
\examples{
# Read the shipped defaults:
pull_covr_env_vars()

}
