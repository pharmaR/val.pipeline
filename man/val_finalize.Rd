% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/val_finalize.R
\name{val_finalize}
\alias{val_finalize}
\title{Finalize a \code{val_build()} Run}
\usage{
val_finalize(
  val_dir = NULL,
  deps = "depends",
  val_start = NULL,
  n_candidates = NULL,
  write_qualified_lists = TRUE,
  render_report = TRUE,
  verbose = NULL,
  config_path = NULL,
  prep = NULL
)
}
\arguments{
\item{val_dir}{Character(1). Path to a run directory produced by
\code{\link[=val_build]{val_build()}} (\verb{R_<ver>/<YYYYMMDD>/}) that contains an
\verb{assessed/} subdirectory with per-package \verb{*_assess_record.rds}
and \verb{*_meta.rds} files. Must exist.}

\item{deps}{Character(1). Dependency edge set to propagate
downgrades along. \code{"depends"} (default) covers Depends + Imports
\itemize{
\item LinkingTo (matches \code{\link[=val_build]{val_build()}}'s default and how the
\verb{_meta.rds} files were built). \code{"suggests"} propagates through
Suggests too. Same semantics as \code{\link[=reject_iteration]{reject_iteration()}}'s \code{deps}
arg.
}}

\item{val_start}{POSIXct or \code{NULL}. Wall-clock start time of the
original \code{val_build()} invocation, used to render a "Build Xh Xm
Xs" summary line and to compute \code{pipeline_runtime} for the
summary report. When \code{NULL} (default; the case when
\code{val_finalize()} is called ad-hoc from a fresh session), the
wall-clock line and \code{pipeline_runtime} are skipped.}

\item{n_candidates}{Integer(1) or \code{NULL}. Pre-filter candidate-set
size, forwarded to \code{\link[=val_pipeline_report]{val_pipeline_report()}}. When \code{NULL}
(default), the report auto-derives it from
\code{pre_filtered_pkg_metrics.rds} in \code{val_dir} if present.}

\item{write_qualified_lists}{Logical(1). When \code{TRUE} (default),
calls \code{\link[=write_qualified_pkg_lists]{write_qualified_pkg_lists()}} to produce the per-source
\verb{qualified-<src>.txt} / \verb{blocklist-<src>.txt} files. Failure is
downgraded to a warning; the collated qual_* RDS files are
already on disk regardless.}

\item{render_report}{Logical(1). When \code{TRUE} (default), calls
\code{\link[=val_pipeline_report]{val_pipeline_report()}} to render the HTML + PDF summary report.
Failure is downgraded to a warning.}

\item{verbose}{Console verbosity control. One of \code{"quiet"},
\code{"minimal"}, \code{"normal"} (default), or \code{"verbose"}. See the
\code{val.pipeline} verbosity docs for tier definitions.}

\item{config_path}{Optional path to a user-supplied \code{config.yml}.
When provided, every internal \code{\link[=pull_config]{pull_config()}} call in this run
reads from that file. When \code{NULL} (default), \code{val_finalize()}
uses the \code{config.yml} copy that \code{\link[=val_build]{val_build()}} snapshotted next
to \code{val_dir} (if present) — matching the config the assessment
loop actually ran under. Falls back to the packaged config when
neither is present.}

\item{prep}{Optional \code{val_prep} object returned by
\code{\link[=val_prep_pipeline]{val_prep_pipeline()}}. When supplied, its fields become the
defaults for \code{val_dir}, \code{val_start}, \code{n_candidates}, \code{deps},
\code{config_path}, and \code{verbose} — so
\code{val_finalize(prep = prep)} is the recommended recovery
one-liner after a two-phase run
(\code{val_pipeline(prep = prep, finalize = FALSE)}). Any argument
passed explicitly to \code{val_finalize()} overrides the corresponding
\code{prep} field. When \code{NULL} (default), every argument comes from
its usual place.}
}
\value{
Invisibly, a list with element \code{val_dir} (character path).
}
\description{
Takes a run directory whose per-package \verb{_assess_record.rds} and
\verb{_meta.rds} files have already been produced by \code{\link[=val_build]{val_build()}} and
produces the run-level artifacts on top of them:
}
\details{
\enumerate{
\item \code{qual_assessments.rds}  — every package's riskmetric record,
\code{dplyr::bind_rows()}-collated from \verb{assessed/*_assess_record.rds}.
\item \code{qual_metadata0.rds}    — interim per-package qualification
frame, \code{dplyr::bind_rows()}-collated from \verb{assessed/*_meta.rds}.
Kept as a separate file so the pre-propagation state remains
inspectable for debugging decision-graph issues.
\item \code{qual_metadata.rds}     — final per-package qualification
frame after \code{\link[=reject_iteration]{reject_iteration()}} propagates dep-driven
downgrades (a package inherits its worst-off dependency's
decision).
\item Rewritten \verb{assessed/*_meta.rds} files for every package
whose \code{decision} changed under propagation, so per-package
reports pick up the new \code{final_decision} /
\code{final_decision_reason} / \code{final_decision_reason_note}.
\item \code{timings.csv}           — long-format per-phase timings
built in the same streaming pass as \code{qual_metadata0.rds}. Feed
this to \code{\link[=val_timings_summary]{val_timings_summary()}} for profiling.
\item \verb{qualified-<src>.txt} / \verb{blocklist-<src>.txt} (opt-out via
\code{write_qualified_lists = FALSE}) — per-source PPM provisioning
files via \code{\link[=write_qualified_pkg_lists]{write_qualified_pkg_lists()}}.
\item HTML + PDF summary report (opt-out via \code{render_report = FALSE}) — high-level run summary via \code{\link[=val_pipeline_report]{val_pipeline_report()}}.
}

\code{val_finalize()} is the tail half of what used to be a monolithic
\code{\link[=val_build]{val_build()}} before val.pipeline v0.1.21. Splitting it out means:

\itemize{
\item A \code{val_build()} that hangs / gets killed / OOMs \emph{after} the
per-package assessment loop but before the collation runs can
now be recovered from disk without re-running the 40h
assessment: fresh session, \code{val_finalize("<val_dir>")}, done.
\item Ad-hoc callers who want to poke at the raw per-package RDS
files (e.g. selectively rerun a handful of packages via
\code{\link[=val_pkg]{val_pkg()}}) can now regenerate the collated artifacts on
demand.
}

See #101 for the split rationale and the hang symptom that
motivated it.
}
\examples{
\dontrun{
# Preferred recovery flow — one object drives both phases:
prep <- val_prep_pipeline()
val_pipeline(prep = prep, finalize = FALSE)
# ...same or a fresh session, later:
val_finalize(prep = prep)

# Ad-hoc, when all you have is the run directory on disk:
val_finalize("/data/pm/riskassessments/R_4.5.2/20260721")

# Skip the PPM provisioning files + summary report for a quick
# collate-only pass (useful when iterating on decision logic):
val_finalize("...", write_qualified_lists = FALSE, render_report = FALSE)
}

}
