% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{pretty_rule_condition}
\alias{pretty_rule_condition}
\title{Pretty-Format a Single riskmetric Rule Condition}
\usage{
pretty_rule_condition(expr_str)
}
\arguments{
\item{expr_str}{A single character scalar. \code{NA} or empty strings return
\code{NA_character_}.}
}
\value{
A single character string, or \code{NA_character_} for empty input.
}
\description{
Converts a rule condition expression (as returned by
\code{\link[=build_decisions_df]{build_decisions_df()}} in the \code{condition} / \code{auto_accept} columns) into a
short, human-readable string suitable for display in a threshold summary
table. Used by the summary report template
(\code{inst/report/summary/summary_template.qmd}).
}
\details{
Supported patterns:
\itemize{
\item \code{~ .x > n} -> \code{"> n"} (with thousands separators for numeric \code{n})
\item \code{~ .x < n}, \code{>=}, \code{<=}, \code{==}, \code{!=} similarly (using Unicode
symbols for \code{>=}, \code{<=}, \code{!=})
\item \code{~ dplyr::between(.x, a, b)} -> \code{"a - b"}
\item \verb{~ is.na(.x) | <cond>} -> \code{"<cond> or NA"}
\item \code{~ is.na(.x)} (alone) -> \code{"NA"}
\item Any other expression is returned as-is with the leading \code{~} stripped.
}
}
\keyword{internal}
