% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbosity.R
\name{val_time_block}
\alias{val_time_block}
\title{Time an expression and record the elapsed seconds}
\usage{
val_time_block(label, expr)
}
\arguments{
\item{label}{Character(1). Short identifier for the block, e.g.
\code{"assess_final"}, \code{"report"}. Used as the list key in the
accumulator and as the tag in the emitted log line.}

\item{expr}{An unevaluated R expression, typically supplied in
braces: \verb{val_time_block("report", \{ <expensive call> \})}.}
}
\value{
The value of \code{expr}.
}
\description{
Evaluates \code{expr}, \code{val_msg()}'s a \code{"[timing] <label>: N.NNs"} line
at the \code{"normal"} tier, and appends the elapsed seconds to a
per-package accumulator stored in
\code{options("val.pipeline.pkg_timings")} (a named list mapping label
-> numeric vector of seconds). \code{\link[=val_pkg]{val_pkg()}} resets this accumulator
at the top of every package assessment and attaches its final
contents to the returned \code{meta_list} as \verb{$timings}; \code{\link[=val_build]{val_build()}}
then aggregates all packages' \verb{$timings} into a \code{timings.csv}
under \code{val_dir}.
}
\details{
If the same \code{label} is fenced more than once inside a single
package (e.g. an "initial" and "final" assess pass both fencing
\code{"assess"}), both durations are retained in insertion order --
downstream aggregation can sum, mean, or explode as needed.

Timings are recorded even when \code{expr} errors, so a failure inside
a fenced block still surfaces "we spent N seconds here before it
blew up" in the log / csv.
}
\keyword{internal}
