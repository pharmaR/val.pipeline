% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{capture_covr_skip_report}
\alias{capture_covr_skip_report}
\title{Capture a \code{testthat} Skip Report for a Package Source}
\usage{
capture_covr_skip_report(pkg_source_path, env_vars = character(0))
}
\arguments{
\item{pkg_source_path}{Character(1). Path to an extracted package
source (i.e. the directory that contains \code{DESCRIPTION}).}

\item{env_vars}{Named character vector of environment variables to
set for the test run via \code{\link[withr:with_envvar]{withr::with_envvar()}}. Typically the
output of \code{\link[=pull_covr_env_vars]{pull_covr_env_vars()}} so the standalone testthat run
sees the same env as the covr run and reports the same skip
population. Empty by default.}
}
\value{
\code{NULL} on any failure, otherwise a list with two elements:
\describe{
\item{\code{totals}}{A named list of integers: \code{n_test} (total
\code{test_that()} blocks executed), \code{n_pass} (blocks where every
expectation passed), \code{n_fail} (blocks with at least one
failure), \code{n_skip} (blocks that hit a \verb{testthat::skip*()}
call), \code{n_error} (blocks that errored), \code{n_warn} (blocks that
emitted a warning expectation).}
\item{\code{top_reasons}}{A \code{data.frame} with columns \code{reason}
(character) and \code{n} (integer), sorted by \code{n} descending,
truncated to the top 10 skip messages. Empty
(\code{0}-row) when no tests were skipped.}
}
}
\description{
Runs \code{testthat::test_dir()} on \verb{<pkg_source_path>/tests/testthat/}
under a controlled environment and returns a structured summary of
how many tests were skipped and why. \code{val_pkg()} uses this to
surface \emph{why} a package's \code{covr_coverage} came in lower than the
maintainer's GitHub badge suggests — the top drivers are almost
always \code{testthat::skip_on_cran()}, \code{testthat::skip_if_offline()},
\code{testthat::skip_if_not_installed("<Suggests>")}, or a
maintainer-invented \code{Sys.getenv("<flag>")} guard.
}
\details{
The report is aggregated per test \emph{block} (i.e. one \code{test_that()}
call), not per expectation. A skipped block counts once. This
matches how a reviewer thinks about "how many tests were skipped"
and avoids inflating counts when a skipped block would have
contained many expectations.

Failure modes are absorbed silently — a broken test suite or a
\code{testthat} version mismatch must never derail \code{val_pkg()}. The
function returns \code{NULL} when:
\itemize{
\item \verb{pkg_source_path/tests/testthat/} does not exist (e.g. the
package uses \code{tinytest}, \code{RUnit}, or no tests at all — tinytest /
RUnit support is a deliberate follow-up),
\item \code{testthat::test_dir()} errors for any reason.
}

The caller should treat \code{NULL} as "unknown" and stamp the
downstream aggregate columns as \code{NA}.
}
\examples{
\dontrun{
# Called from val_pkg() right after the final pkg_assess() call:
env <- pull_covr_env_vars()
capture_covr_skip_report(file.path(sourced, pkg), env_vars = env)
}

}
