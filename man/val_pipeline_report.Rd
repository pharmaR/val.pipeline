% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/val_pipeline_report.R
\name{val_pipeline_report}
\alias{val_pipeline_report}
\title{Generate a high-level Quarto summary report for a validation run}
\usage{
val_pipeline_report(
  qual_metadata_path,
  qual_assessments_path = NULL,
  out_dir = NULL,
  format = c("html", "pdf"),
  file_stem = "val_pipeline_summary",
  title = NULL,
  subtitle = NULL,
  n_candidates = NULL,
  pre_filtered_path = NULL,
  quiet = FALSE
)
}
\arguments{
\item{qual_metadata_path}{Character. Path to a \code{qual_metadata.rds} file
produced by \code{\link[=val_build]{val_build()}}. Required.}

\item{qual_assessments_path}{Character or NULL. Optional path to the
sibling \code{qual_assessments.rds}. When \code{NULL} (the default), the function
looks for \code{qual_assessments.rds} in the same directory as
\code{qual_metadata_path}; pass \code{NA} to skip this lookup and produce a
metadata-only report.}

\item{out_dir}{Character. Directory to write the rendered report into.
Defaults to the directory containing \code{qual_metadata_path}.}

\item{format}{Character vector. One or more of \code{"html"}, \code{"pdf"}. Defaults
to both.}

\item{file_stem}{Character. Base filename (without extension) for the
rendered report. Defaults to \code{"val_pipeline_summary"}.}

\item{title}{Character. Optional report title. When \code{NULL}, a title is
derived from the validation date on the input.}

\item{subtitle}{Character. Optional subtitle (e.g. "R 4.5.2 / 2026-06-21
snapshot"). When \code{NULL}, a subtitle is derived from the input.}

\item{n_candidates}{Integer or NULL. Number of candidate packages
considered by \code{\link[=val_pipeline]{val_pipeline()}} before the \code{remote_reduce} filter (i.e.
\code{nrow(pre_filtered_pkg_metrics)}). When \code{NULL} (the default), the
function looks for a sibling \code{pre_filtered_pkg_metrics.rds} in the
directory containing \code{qual_metadata_path} and uses its row count. Pass
\code{NA} to explicitly skip this metric.}

\item{pre_filtered_path}{Character or NULL. Optional path to a
\code{pre_filtered_pkg_metrics.rds} produced by \code{\link[=val_pipeline]{val_pipeline()}} (the
pre-\code{val_build()} candidate-set data frame with a \code{package} and
\code{final_risk} column). When \code{NULL} (the default), a sibling file of
that name next to \code{qual_metadata_path} is used if present. Pass \code{NA}
to explicitly skip and omit the Pre-Filter Summary section.}

\item{quiet}{Logical. Suppress Quarto rendering output. Default \code{FALSE}.}
}
\value{
Invisibly, a character vector of the rendered report file paths.
}
\description{
Renders an HTML and/or PDF summary of a \code{val_pipeline()} / \code{val_build()}
run, intended for GxP / QMS archival. Given a \code{qual_metadata.rds} produced
by \code{\link[=val_build]{val_build()}}, it summarises the final decisions, downgrades due to
dependencies, per-category package lists, and runtime. When the sibling
\code{qual_assessments.rds} is also supplied, the report adds metric-level
distributions (coverage buckets, R CMD check outcomes, license mix, etc.).
}
\details{
The function is exported so it can be re-run outside the pipeline (e.g.
against a historical run archived under \verb{/data/shared/riskassessments/}),
and is also invoked automatically at the end of \code{\link[=val_pipeline]{val_pipeline()}} so every
full run leaves a summary artefact next to its evidence files.
}
\section{Backward compatibility with older evidence files}{
The template is deliberately tolerant of older \code{qual_metadata.rds} /
\code{qual_assessments.rds} files so historical runs can still be summarised:
\itemize{
\item \code{decision_reason_note} / \code{final_decision_reason_note} (added by issue
#37) are backfilled with \code{NA} when absent — the per-category package
tables just show a blank Note column for those rows.
\item \code{assessment_runtime_mins} (older files may lack it) — the runtime
section is skipped with a note.
\item Any metric column in \code{qual_assessments.rds} may be absent
(\code{covr_coverage}, \code{license}, \code{r_cmd_check_errors}, ...) — the
corresponding sub-section is skipped.
\item List-typed columns in \code{qual_assessments.rds} (e.g. raw
\code{r_cmd_check} output) are ignored when computing numeric summaries.
}

A minimum set of columns is still required in \code{qual_metadata.rds}:
\code{pkg}, \code{ver}, \code{decision}, \code{decision_reason}, \code{final_decision}, and
\code{final_decision_reason}. Files missing any of these predate
\verb{val.pipeline 0.0.1} and are rejected with an informative error.
}

\examples{
\dontrun{
val_pipeline_report(
  qual_metadata_path = file.path(
    "/data/shared/riskassessments/R_4.5.2/20260621",
    "qual_metadata.rds"
  )
)
}

}
