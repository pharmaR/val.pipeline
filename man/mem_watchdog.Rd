% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mem_watchdog.R
\name{mem_watchdog}
\alias{mem_watchdog}
\title{Per-Package Memory Watchdog Helpers}
\description{
Lightweight helpers that let \code{\link[=val_build]{val_build()}} / \code{\link[=val_pkg]{val_pkg()}} record per-package
peak RSS to a run-scoped TSV (\verb{<val_dir>/mem_watchdog.tsv}). The goal is
pure observability: sizing \code{workers} on future runs from real numbers
instead of eyeballed guesses. See #122.
}
\details{
On Linux, \code{sample_peak_rss_mb()} reads \verb{VmHWM:} from \verb{/proc/<pid>/status},
which is the kernel-tracked high-water mark for that R subprocess since it
was booted. On macOS / Windows we fall back to the (non-peak) current RSS
via \code{ps::ps_memory_info()} — still useful, and clearly documented in the
\code{sampler} column so downstream summaries can flag it.

The TSV is written line-at-a-time with \code{cat(append = TRUE)}; under Linux
NFSv4.2 (Workbench) \code{O_APPEND} writes shorter than PIPE_BUF are atomic, so
concurrent worker appends interleave cleanly. Same reasoning as the run
log wiring landed for #87.
}
\keyword{internal}
