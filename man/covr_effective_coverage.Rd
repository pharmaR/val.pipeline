% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{covr_effective_coverage}
\alias{covr_effective_coverage}
\title{Estimate effective code coverage by projecting skipped tests onto
the covered-line rate of the tests that actually ran}
\usage{
covr_effective_coverage(coverage_pct, pct_skip)
}
\arguments{
\item{coverage_pct}{Numeric(1). Raw \code{covr_coverage} on 0-100 scale
(i.e. \code{pkg_assessment$covr_coverage$totalcoverage} for a
riskmetric-shaped assessment).}

\item{pct_skip}{Numeric(1). Skip rate on 0-100 scale, i.e.
\code{covr_skip_report$totals$n_skip / n_test * 100}.}
}
\value{
Numeric(1) on 0-100 scale, or \code{NA_real_} when either input
is \code{NA}, \code{pct_skip >= 100} (division by zero), or
\code{coverage_pct} is not finite.
}
\description{
Simple, cheap approximation used by \code{\link[=val_pkg]{val_pkg()}} to give reviewers
a rough "would this pkg have passed if the skipped tests had run?"
signal. Formally:
}
\details{
effective = coverage_pct / (1 - pct_skip / 100)

capped at 100. Assumes skipped \code{test_that()} blocks would have
contributed to line coverage at the \emph{average} rate of the blocks
that did run. That is almost never exactly true — skipped tests
often target the exact code that was already uncovered — so the
result is an upper-bound approximation, not ground truth. The
per-package and summary report templates label it as an estimate.
}
\keyword{internal}
