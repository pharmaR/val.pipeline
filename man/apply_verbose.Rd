% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbosity.R
\name{apply_verbose}
\alias{apply_verbose}
\title{Apply a \code{verbose} argument for the duration of the calling function}
\usage{
apply_verbose(verbose)
}
\arguments{
\item{verbose}{The \code{verbose} argument from the calling function.}
}
\value{
Invisibly, the resolved tier string.
}
\description{
Sets \verb{options(val.pipeline.verbose = <resolved>)} and registers an
\code{on.exit()} handler in the calling function's frame to restore the
previous value. Public \verb{val_*} entry points call this once, near the
top, so every internal helper (including ones in \code{R/utils.R} that
aren't given a \code{verbose} arg) can just call \code{val_msg()} and pick up
the correct tier.
}
\keyword{internal}
