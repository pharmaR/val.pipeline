% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbosity.R
\name{pkg_assess_mins}
\alias{pkg_assess_mins}
\alias{pkg_skip_report_mins}
\title{Extract per-phase runtime minutes from the \code{val_time_block()} store}
\usage{
pkg_assess_mins(timings)

pkg_skip_report_mins(timings)
}
\arguments{
\item{timings}{Named list of numeric vectors, shaped like the
output of \code{\link[=get_pkg_timings]{get_pkg_timings()}}. Missing / non-list input coerces
to \code{list()} (both helpers then return \code{NA_real_}).}
}
\value{
Numeric(1) minutes, or \code{NA_real_}.
}
\description{
Helpers that pull specific phase totals out of the map returned by
\code{\link[=get_pkg_timings]{get_pkg_timings()}} and convert them from seconds to minutes.
Factored out of \code{\link[=val_pkg]{val_pkg()}}'s \code{meta_list} construction so tests
exercise the same expressions that populate the meta bundle
(rather than a hand-copied duplicate) -- see the #178 review.
}
\details{
\itemize{
\item \code{pkg_assess_mins()} sums the \code{assess_initial} + \code{assess_final}
phases (the two \code{riskmetric::pkg_assess()} calls). Returns
\code{NA_real_} when neither phase fired (e.g. the \code{reuse_init} /
\code{remote_only} short-circuits in \code{\link[=val_build]{val_build()}}, or a failure
before the initial assessment).
\item \code{pkg_skip_report_mins()} returns the \code{skip_report} phase total
(the \code{capture_covr_skip_report()} \code{testthat::test_dir()} replay)
or \code{NA_real_} when the phase didn't fire (skip-report disabled,
coverage above threshold, or the package is in
\code{covr_skip_report$skip_pkgs}).
}

Both helpers take an already-materialised timings map (rather than
calling \code{\link[=get_pkg_timings]{get_pkg_timings()}} themselves) so a single val_pkg()
meta_list construction only pulls the option once. Tests may pass
a hand-built map for controlled input.
}
\keyword{internal}
