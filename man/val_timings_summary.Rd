% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/val_timings_summary.R
\name{val_timings_summary}
\alias{val_timings_summary}
\title{Summarize \code{timings.csv} from a \code{val_build()} Run}
\usage{
val_timings_summary(x, top_n = 10L, quiet = !interactive())
}
\arguments{
\item{x}{One of:
\itemize{
\item Character(1) path to a \code{timings.csv} file.
\item Character(1) path to a run directory (\verb{R_<ver>/<YYYYMMDD>/})
that contains a \code{timings.csv} — the file is located
automatically.
\item A data.frame / tibble already read into R, as long as it
has columns \code{pkg}, \code{phase}, and \code{seconds}. A \code{ver} column is
optional and passed through when present.
}}

\item{top_n}{Integer. When printing the interactive summary, how
many packages to show in the top-total table. \code{0} disables the
print step entirely (useful when embedding the call inside another
pipeline). Default \code{10}.}

\item{quiet}{Logical. When \code{TRUE}, suppresses the interactive
print step regardless of \code{top_n}. Default \code{!interactive()}.}
}
\value{
Invisibly, a list of three tibbles:
\describe{
\item{\code{per_pkg}}{One row per package: \code{pkg}, \code{ver} (if present),
\code{total_s}, \code{n_phases}. Sorted descending by \code{total_s}.}
\item{\code{per_phase}}{One row per phase across all packages: \code{phase},
\code{total_s}, \code{mean_s}, \code{median_s}, \code{p95_s}, \code{n_pkgs}. Sorted
descending by \code{total_s}.}
\item{\code{wide}}{One row per package, one column per phase.
Missing (pkg, phase) combinations come through as \code{NA}.}
}
}
\description{
Reads the per-package / per-phase timings that \code{\link[=val_build]{val_build()}} writes
to \code{timings.csv} (long format: \code{pkg}, \code{ver}, \code{phase}, \code{seconds};
introduced in #87) and returns three ready-to-inspect tibbles:
\code{per_pkg} (total wall time per package), \code{per_phase} (aggregate
stats per phase across all packages), and \code{wide} (one row per
package, one column per phase, missing phases as \code{NA}).
}
\details{
This is a \strong{standalone analysis helper} — it does not depend on
any state produced by \code{\link[=val_pipeline]{val_pipeline()}}. Point it at a
\code{timings.csv}, a run directory that contains one, or a data frame
already read into R, and it just works. Suitable for both
interactive profiling of a completed run and cross-run comparison
(call it once per run, then diff the returned tibbles).

When called interactively without an explicit \code{top_n = 0}, the
helper also prints a short top-N summary (top-N pkgs by total
seconds + phase-level aggregates) to make it useful as a one-liner
from the console.
}
\examples{
\dontrun{
# From a completed run directory:
val_timings_summary("/data/shared/riskassessments/R_4.5.2/20260621")

# Direct path to the CSV works too:
res <- val_timings_summary(
  "/data/shared/riskassessments/R_4.5.2/20260621/timings.csv",
  top_n = 20
)
res$per_phase

# Cross-run diff:
a <- val_timings_summary("R_4.5.2/20260621", quiet = TRUE)
b <- val_timings_summary("R_4.5.2/20260721", quiet = TRUE)
dplyr::full_join(a$per_phase, b$per_phase,
                 by = "phase", suffix = c("_a", "_b"))
}

}
